
// to get the MySQL imgae and run it at the firs ttime if you don't have 

docker run -it -e MYSQL_ROOT_PASSWORD=admin --hostname=admin -p 3306:3306 mysql:8.3.0



// the new one with the network and also the script execution  for the my sql image 

docker run -it -e MYSQL_ROOT_PASSWORD=admin --hostname=admin -p 3307:3306 --network=ProductsNetwork -v "D:\Micro Services Course\Mine\sql1.sql:/docker-entrypoint-initdb.d/sql1.sql" mysql:8.3.0




==> and this is for the product docker image run 

docker run -p 8080:8080 --network=ProductsNetwork -e MYSQL_HOST=admin -e MYSQL_PASSWORD=admin -e PortNumber=3306  heshamayman0/productimage:V1.0