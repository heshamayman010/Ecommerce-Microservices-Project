docker run -it -e MYSQL_ROOT_PASSWORD=admin --hostname=admin -p 3306:3306 mysql:8.3.0

the new one with the network and also the script execution 

docker run -it -e MYSQL_ROOT_PASSWORD=admin --hostname=admin -p 3307:3306 --network=ProductsNetwork -v "E:/ecommerce using micro/ProductsMangement/DatabaseScript:/docker-entrypoint-initdb.d" mysql:8.3.0



==> and this is for the product docker image run 

docker run -p 8080:8080 --network=ProductsNetwork -e MYSQL_HOST=admin -e MYSQL_PASSWORD=admin -e PortNumber=3306  heshamayman0/ecommerce.productmangement:V1.0

